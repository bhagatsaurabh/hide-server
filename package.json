{
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml up --build -d",
    "dashboard": "kubectl -n kubernetes-dashboard port-forward svc/kubernetes-dashboard-kong-proxy 8443:443"
  }
}
