services:
  redis:
    image: redis:8-alpine
    container_name: redis
    expose:
      - 6379
    command: ["redis-server", "--notify-keyspace-events", "Ex"]
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512m
