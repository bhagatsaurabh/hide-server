services:
  gateway:
    extends:
      file: docker-compose.yml
      service: gateway
    build:
      context: .
      target: production
    environment:
      - EUREKA_HOST=host.docker.internal
      - EUREKA_PORT=8761
    command: node dist/main.js
