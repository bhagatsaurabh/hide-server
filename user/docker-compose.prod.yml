services:
  user:
    extends:
      file: docker-compose.yml
      service: user
    build:
      context: .
      target: production
    environment:
      - EUREKA_HOST=host.docker.internal
      - EUREKA_PORT=8761
      - SERVICE_NAME=USER
      - SERVICE_PORT=3002
    command: node dist/main.js
