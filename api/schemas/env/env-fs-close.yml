EnvFSClose:
  type: object
  required: [service, action, payload, correlationId]
  properties:
    service:
      type: string
      const: env
    action:
      type: string
      enum: [fs.close]
    correlationId:
      type: string
    payload:
      type: object
      required: [path, uuid]
      properties:
        path:
          type: string
        ino:
          type: integer
          nullable: true
        uuid:
          type: string
