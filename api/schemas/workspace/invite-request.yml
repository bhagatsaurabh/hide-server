InviteRequest:
  type: object
  required: [inviteeId, workspaceUUID]
  properties:
    inviteeId:
      type: string
    workspaceUUID:
      type: string
    sshKey:
      type: string
      nullable: true
