services:
  provisioner:
    expose:
      - 80
    build:
      context: .
      dockerfile: dockerfile
      target: development
    image: hide-server-provisioner
    volumes:
      - .:/app
      - ~/.kube:/root/.kube
      - /app/.build
    environment:
      - SERVICE_PORT=80
      - K8S_API_SERVER=https://host.docker.internal:6443
    command: [ "/bin/sh", "-c", "air -c .air.toml" ]
