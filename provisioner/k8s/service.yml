apiVersion: v1
kind: Service
metadata:
  name: provisioner
spec:
  selector:
    app: provisioner
  ports:
    - name: api
      protocol: TCP
      port: 80
      targetPort: 80
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
  type: ClusterIP
