apiVersion: v1
kind: PersistentVolume
metadata:
  name: rmq-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/rmq
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
