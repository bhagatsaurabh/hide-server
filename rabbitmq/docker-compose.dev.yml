services:
  rabbitmq:
    image: rabbitmq:4-management-alpine
    container_name: rabbitmq
    expose:
      - 5672
    ports:
      - 15672:15672
    volumes:
      - ./.rabbitmq-data:/var/lib/rabbitmq
