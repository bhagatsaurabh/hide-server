apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-pv
spec:
  storageClassName: local-path
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /var/hide/grafana-data
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
